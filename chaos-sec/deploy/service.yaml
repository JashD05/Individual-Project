apiVersion: v1
kind: Service
metadata:
  name: chaos-sec
  namespace: chaos-sec-experiments
  labels:
    app: chaos-sec
spec:
  selector:
    app: chaos-sec
  ports:
    - name: siem
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
